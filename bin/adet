#!/usr/bin/env perl

use strict;
use warnings;
use lib 'lib';
use lib '../adet/lib';

use App::adet;
use App::adet::UI::cmdline;

my $ui = App::adet::UI::cmdline->create( \@ARGV, \%ENV );
App::adet->run( $ui );